#include <stdio.h>

int main(void)
{
    int i = 0;
    while (i >= 0)
    {
        printf("%i\n", i);
        i++;
    }
}

/*
isso abaixo eh a mesma coisa, so tirou os parenteses mas cuidado com os ; e indentacao
somente vale para quando eh uma linha de apenas

#include <stdio.h>

int main(void)
{
    for (int i = 0; i <= 50; i++)
        printf("%i\n", i);
}


*/
